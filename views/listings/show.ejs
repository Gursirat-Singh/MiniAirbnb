<% layout('/layouts/boilerplate') -%>
<div class="row">
  <div class="col-8 offset-2 mb-5 mt-3">
    <div class="card border-0">
      <div class="col-8 mx-auto offset-2">
        <h1 class="mb-4"><%=listing.title%></h1>
        <img
          src="<%=listing.image.url%>"
          alt="Stay Image"
          class="card-img-top rounded-4 mb-3"
          style="height: 30vh; object-fit: cover"
        />

        <h2>Owned By: <i><%=listing.owner[0].username%></i></h2>
        <p class="text-secondary my-3"><%=listing.description%></p>
        <h3 class="mb-3">₹<%=listing.price.toLocaleString("en-IN")%></h3>
        <%=listing.location%>, <%=listing.country%> <% if(currentUser &&
        currentUser._id.equals(listing.owner[0]._id)){%>
        <div class="btns">
          <a href="/listings/<%=listing._id%>/edit" class="btn btn-danger px-4"
            >Edit</a
          >
          <form
            class="d-inline"
            action="/listings/<%=listing._id%>?_method=delete"
            method="post"
          >
            <button class="btn btn-dark px-4">Delete</button>
          </form>
        </div>
        <%}%>
      </div>
    </div>
    <div class="col-8 mx-auto offset-2 mt-5 mb-4">
      <hr class="my-4 w-100" />
      <%if(currentUser){%>
      <h4 class="mb-4">Leave a Review</h4>
      <form
        action="/listings/<%=listing._id%>/reviews"
        method="POST"
        novalidate
        class="mb-4 needs-validation"
      >
        <div class="mb-4 mt-4">
          <legend>Rating</legend>
          <fieldset class="starability-slot">
            <input
              type="radio"
              id="no-rate"
              class="input-no-rate"
              name="review[rating]"
              value="1"
              checked
              aria-label="No rating."
            />
            <input
              type="radio"
              id="first-rate1"
              name="review[rating]"
              value="1"
            />
            <label for="first-rate1" title="Terrible">1 star</label>
            <input
              type="radio"
              id="first-rate2"
              name="review[rating]"
              value="2"
            />
            <label for="first-rate2" title="Not good">2 stars</label>
            <input
              type="radio"
              id="first-rate3"
              name="review[rating]"
              value="3"
            />
            <label for="first-rate3" title="Average">3 stars</label>
            <input
              type="radio"
              id="first-rate4"
              name="review[rating]"
              value="4"
            />
            <label for="first-rate4" title="Very good">4 stars</label>
            <input
              type="radio"
              id="first-rate5"
              name="review[rating]"
              value="5"
            />
            <label for="first-rate5" title="Amazing">5 stars</label>
          </fieldset>
        </div>
        <div class="mb-4">
          <label for="comment" class="form-label">Comments</label>
          <textarea
            name="review[comment]"
            id="comment"
            rows="5"
            required
            class="form-control"
            placeholder="Write your experience..."
          ></textarea>
          <div class="valid-feedback">Thanks for comments</div>
          <div class="invalid-feedback">Please Enter Comments</div>
        </div>
        <button class="btn btn-success">Submit Review</button>
      </form>
      <hr class="my-4 w-100" />
      <%}%>
    </div>
    <%if(listing.reviews.length > 0 ){%>
    <div class="row">
      <div class="col-8 mx-auto offset-2 mb-4">
        <h3 class="mb-4">All Reviews</h3>
        <div class="row row-cols-1 row-cols-md-1 g-4">
          <%for(review of listing.reviews){%>
          <div class="col">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h5 class="card-title">
                  <div
                    class="d-flex justify-content-between align-items-center"
                  >
                    <div>
                      <i class="fa-regular fa-circle-user me-2"></i
                      ><%=review.author[0].username%>
                    </div>
                    <div class="d-flex align-items-center gap-2">
                      <p
                        class="starability-result"
                        data-rating="<%=review.rating%>"
                      ></p>
                    </div>
                  </div>
                </h5>
                <p class="card-text text-muted mb-3"><%=review.comment%></p>
                <form
                  action="/listings/<%=listing._id%>/reviews/<%=review._id%>?_method=Delete"
                  method="post"
                  class="d-inline"
                >
                  <button class="btn btn-sm btn-dark">
                    <i class="fa-regular fa-trash-can me-1"></i>Delete
                  </button>
                </form>
              </div>
            </div>
          </div>
          <%}%>
        </div>
      </div>
      <hr class="my-4 w-100" />
      <%}%>
      <div class="col-8 mx-auto offset-2 mt-4">
        <h3>Location on Map</h3>
        <div id="map" data-location="<%= listing.location %>"></div>
      </div>
    </div>
  </div>
</div>

<script src="/javascript/map.js"></script>
